# CONTEXT.md — 客服违禁词桌面监控工具

> 领域术语与决策记录。实现细节不入此文件，只记"是什么、不是做什么"。

## 角色与组件

- **监控客户端 (Client)**：部署在每台客服终端的轻量 Windows 程序，系统托盘常驻、后台静默运行，捕获指定软件输入并匹配违禁词、触发告警。
- **词库编辑工具 (Admin Tool)**：管理员桌面上运行的轻量程序（非服务），维护**词库**（增删改查/导入导出），导出权威 `wordlib.json` 供分发。
- **目标软件 (Target Software)**：被监控的客服系统/聊天工具，由用户按**进程 EXE 名**配置。非目标软件的输入不受影响。
- **本地词库 (Local Word Library)**：客户端读取与其同目录/约定路径下的 `wordlib.json`；用 `FileSystemWatcher` 监听变更热重载，离线天然可用。
- **文件分发 (File Distribution)**：管理员把 `wordlib.json` 通过任意手段（可访问的共享目录、U 盘、邮件、组策略/GPO）下发到各客户端；不依赖运行时服务器。
- **自动拉取（可选增强）**：仅在局域网可靠环境，客户端可额外定时从共享目录拉取词库；此为可选项，非必需。
- **监控引擎 (Monitoring Engine)**：客户端内负责"捕获文本 + 匹配词库 + 触发告警"的核心模块。

## 捕获技术

- **UI Automation (UIA)**：Windows 无障碍 API。监控引擎用它监听指定进程输入框的 `TextChanged` 事件，直接读取已组合完成的中文文本。这是中文输入下唯一可靠的中文文本来源。
- **键盘钩子 (Keyboard Hook)**：低级全局钩子（`WH_KEYBOARD_LL`）。仅作辅助活跃判断（前台进程是否在输入），**不**用于还原中文文本。

## 词库与匹配

- **违禁词 (Forbidden Word)**：词库中需检测并告警的词条。
- **词库 (Word Library)**：可增删改查、可导入导出的违禁词集合，由**词库编辑工具**维护并导出 `wordlib.json` 分发。
- **匹配 (Matching)**：用 **Aho-Corasick 多模自动机**一次扫描找出文本中所有违禁词命中，词库规模大时也保持实时。
- **词库存储 (Library Storage)**：词库以 **`wordlib.json`**（JSON）落盘，随客户端本地存放，便于文件分发与导入导出；由程序 UI 管理，非人工手改。
- **日志存储 (Log Storage)**：监控日志以 **SQLite** 落盘，由程序内"时间范围检索" UI 访问，终端用户不接触底层文件。

## 告警

- **告警 (Alert)**：命中违禁词时触发的提示，包含**弹窗、声音、高亮**三种方式，每种可在配置中独立开关。
- **高亮标记 (Highlight)**：仅在**本工具的告警弹窗与日志列表**内把违禁词标红；不覆盖/不干扰目标软件窗口。
- **处理结果 (Disposition)**：告警后记录的处置状态，取值为 已弹窗 / 已响铃 / 已记日志 / 客服已确认。系统**仅告警、不拦截**消息发送（见 ADR 0003）。

## 日志

- **监控日志 (Audit Log)**：记录每次违禁词触发的 时间 / 目标软件 / 触发内容 / 处理结果，支持按时间范围检索。
